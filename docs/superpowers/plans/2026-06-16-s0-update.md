# S0-Update: Hallenplan + Lernziele im Intro-Overlay — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Das `#tutorial-start-overlay` um einen SVG-Hallenplan (4 TCP/IP-Zonen) und 5 Lernziele (LZ-Badge + Kurzbeschreibung) in einem 2-Spalten-Layout erweitern.

**Architecture:** Reine HTML/CSS-Änderung. Kein neuer Game-State, kein JS-Eingriff. Bestehender Button `#tutorial-start-btn` → `startTutorial()` bleibt unverändert. Neue CSS-Klassen unter dem Namensraum `.s0-*` isoliert.

**Tech Stack:** Vanilla HTML, CSS Grid, SVG (inline), Playwright (Tests)

---

## Datei-Übersicht

| Datei | Aktion | Was ändert sich |
|---|---|---|
| `tests/ui.spec.js` | Modify | Veraltete IDs korrigieren + neue Tests für SVG + Lernziele |
| `css/style.css` | Modify | 9 neue `.s0-*`-Klassen + Media Query am Dateiende anfügen |
| `index.html` | Modify | Max-Text kürzen + `.s0-two-col`-Div mit SVG und Lernziele-Liste einfügen |

---

## Task 1: Veraltete Tests korrigieren

Die bestehenden Tests referenzieren `#intro-overlay` und `#intro-start-btn`, die im HTML nicht existieren. Die tatsächlichen IDs sind `#tutorial-start-overlay` und `#tutorial-start-btn`.

**Files:**
- Modify: `tests/ui.spec.js`

- [ ] **Schritt 1.1: Tests laufen lassen und Fehler bestätigen**

```bash
npm test
```

Erwartet: Mindestens 2 Fehler — `#intro-overlay` und `#intro-start-btn` werden nicht gefunden.

- [ ] **Schritt 1.2: Veraltete IDs in tests/ui.spec.js korrigieren**

Öffne `tests/ui.spec.js`. Ersetze alle Vorkommen:

| Alt | Neu |
|---|---|
| `#intro-overlay` | `#tutorial-start-overlay` |
| `#intro-start-btn` | `#tutorial-start-btn` |

Die betroffenen Tests (Zeilen ca. 9–28):

```js
test('Intro-Overlay ist beim Laden sichtbar', async ({ page }) => {
  const overlay = page.locator('#tutorial-start-overlay');
  await expect(overlay).toBeVisible();
  await expect(overlay).not.toHaveClass(/hidden/);
});

test('Intro-Button schließt das Overlay', async ({ page }) => {
  await page.locator('#tutorial-start-btn').click();
  await expect(page.locator('#tutorial-start-overlay')).toHaveClass(/hidden/);
});
```

- [ ] **Schritt 1.3: Korrigierte Tests laufen lassen**

```bash
npm test
```

Erwartet: Alle bestehenden Tests PASS (keine Fehler mehr durch falsche IDs).

- [ ] **Schritt 1.4: Commit**

```bash
git add tests/ui.spec.js
git commit -m "fix: korrigiere veraltete Overlay-IDs in Playwright-Tests"
```

---

## Task 2: Neue Playwright-Tests für S0-Elemente schreiben

Neue Tests, die zuerst FAIL (da HTML noch nicht geändert), dann nach Task 4 PASS.

**Files:**
- Modify: `tests/ui.spec.js`

- [ ] **Schritt 2.1: Neue Test-Gruppe am Ende von tests/ui.spec.js anfügen**

Füge nach dem letzten `});` in `tests/ui.spec.js` an:

```js
test.describe('S0-Update: Hallenplan + Lernziele', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });

  test('SVG-Hallenplan ist im Intro-Overlay vorhanden', async ({ page }) => {
    const svg = page.locator('#tutorial-start-overlay svg#s0-map');
    await expect(svg).toBeAttached();
  });

  test('Hallenplan enthält 4 Zonen-Rechtecke (ohne Außenrahmen und Eingang)', async ({ page }) => {
    // Der SVG hat: 1 Außenrahmen + 1 Eingang + 4 Zonen = 6 rects gesamt
    // Zonen-Rects haben ein data-zone-Attribut
    const zones = page.locator('#s0-map rect[data-zone]');
    await expect(zones).toHaveCount(4);
  });

  test('Lernziele-Liste enthält genau 5 Einträge', async ({ page }) => {
    const items = page.locator('#tutorial-start-overlay .s0-lz-item');
    await expect(items).toHaveCount(5);
  });

  test('Jeder Lernziel-Eintrag hat ein LZ-Badge', async ({ page }) => {
    const badges = page.locator('#tutorial-start-overlay .s0-lz-badge');
    await expect(badges).toHaveCount(5);
    const texts = await badges.allTextContents();
    expect(texts).toEqual(['LZ1', 'LZ2', 'LZ3', 'LZ4', 'LZ5']);
  });

  test('Tutorial-Button funktioniert weiterhin nach S0-Update', async ({ page }) => {
    await page.locator('#tutorial-start-btn').click();
    await expect(page.locator('#tutorial-start-overlay')).toHaveClass(/hidden/);
  });
});
```

- [ ] **Schritt 2.2: Neue Tests laufen lassen — müssen FAIL**

```bash
npm test
```

Erwartet: Die 4 neuen S0-Tests FAIL (SVG und `.s0-lz-item` existieren noch nicht). Der Button-Test PASS (Button war schon vorhanden).

- [ ] **Schritt 2.3: Commit (rote Tests)**

```bash
git add tests/ui.spec.js
git commit -m "test: füge fehlschlagende S0-Tests für Hallenplan und Lernziele hinzu"
```

---

## Task 3: CSS-Klassen für das 2-Spalten-Layout hinzufügen

**Files:**
- Modify: `css/style.css` (Ende der Datei)

- [ ] **Schritt 3.1: Neue CSS-Klassen an das Ende von css/style.css anfügen**

Füge nach der letzten Zeile in `css/style.css` an:

```css
/* ── S0-Update: Hallenplan + Lernziele ───────────────────────────────────── */
.s0-two-col { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-bottom: 20px; }
.s0-map-box { border: 1px solid rgba(245,197,24,0.35); border-radius: 10px; padding: 12px; }
.s0-map-label { font-size: 9px; font-weight: 700; letter-spacing: 1.5px; text-transform: uppercase; color: #f5c518; margin-bottom: 8px; }
.s0-lz-box { border: 1px solid rgba(80,224,160,0.3); border-radius: 10px; padding: 12px; }
.s0-lz-label { font-size: 9px; font-weight: 700; letter-spacing: 1.5px; text-transform: uppercase; color: #50e0a0; margin-bottom: 10px; }
.s0-lz-list { display: flex; flex-direction: column; gap: 7px; }
.s0-lz-item { display: flex; gap: 6px; align-items: flex-start; }
.s0-lz-badge { font-size: 9px; font-weight: 700; color: #64a0ff; background: rgba(100,160,255,0.12); border-radius: 4px; padding: 1px 5px; white-space: nowrap; margin-top: 1px; flex-shrink: 0; }
.s0-lz-item span { font-size: 10.5px; color: #c8d8f0; line-height: 1.4; }
@media (max-width: 480px) { .s0-two-col { grid-template-columns: 1fr; } }
```

- [ ] **Schritt 3.2: Commit**

```bash
git add css/style.css
git commit -m "feat: füge S0 CSS-Klassen für Hallenplan-Spalten-Layout hinzu"
```

---

## Task 4: HTML-Overlay erweitern

**Files:**
- Modify: `index.html` (Zeilen 48–58, `#tutorial-start-overlay`)

- [ ] **Schritt 4.1: Max-Text in .overlay-body kürzen**

Ersetze den Inhalt von `.overlay-body` innerhalb `#tutorial-start-overlay` (aktuell Zeile 53–55):

```html
      <div class="overlay-body">
        Ich bin <strong>Max</strong>, dein Lagerleiter. Diese Halle bildet das <strong>TCP/IP-Schichtenmodell</strong> nach &mdash; vier Bereiche, vier Schichten. Ich führe dich kurz durch die Steuerung.
      </div>
```

- [ ] **Schritt 4.2: .s0-two-col-Div mit SVG und Lernziele nach .overlay-body, vor #tutorial-start-btn einfügen**

Das `#tutorial-start-overlay` soll nach dem Update so aussehen:

```html
  <div id="tutorial-start-overlay">
    <div class="overlay-box">
      <div class="overlay-tag">Steuer-Tutorial &middot; Einführung</div>
      <div class="overlay-avatar">&#128119;</div>
      <div class="overlay-title">Willkommen im TCP/IP-Lagerhaus!</div>
      <div class="overlay-body">
        Ich bin <strong>Max</strong>, dein Lagerleiter. Diese Halle bildet das <strong>TCP/IP-Schichtenmodell</strong> nach &mdash; vier Bereiche, vier Schichten. Ich führe dich kurz durch die Steuerung.
      </div>
      <div class="s0-two-col">
        <!-- Hallenplan -->
        <div class="s0-map-box">
          <div class="s0-map-label">&#128506; Hallenplan</div>
          <svg id="s0-map" viewBox="0 0 180 128" width="100%" height="auto" xmlns="http://www.w3.org/2000/svg">
            <rect x="3" y="3" width="174" height="116" rx="6" fill="none" stroke="rgba(100,160,255,0.25)" stroke-width="1.5"/>
            <rect x="80" y="111" width="20" height="8" rx="2" fill="rgba(100,160,255,0.1)" stroke="rgba(100,160,255,0.4)" stroke-width="1"/>
            <text x="90" y="117.5" font-size="4.5" fill="#64a0ff" text-anchor="middle">Eingang</text>
            <rect data-zone="anwendung" x="7" y="7" width="82" height="50" rx="4" fill="rgba(80,224,160,0.1)" stroke="rgba(80,224,160,0.5)" stroke-width="1"/>
            <text x="48" y="24" font-size="6.5" fill="#50e0a0" text-anchor="middle" font-weight="bold">Anwendung</text>
            <text x="48" y="33" font-size="5" fill="rgba(80,224,160,0.65)" text-anchor="middle">Schicht 4</text>
            <text x="48" y="49" font-size="13" text-anchor="middle">&#128241;</text>
            <rect data-zone="transport" x="93" y="7" width="82" height="50" rx="4" fill="rgba(100,160,255,0.1)" stroke="rgba(100,160,255,0.5)" stroke-width="1"/>
            <text x="134" y="24" font-size="6.5" fill="#64a0ff" text-anchor="middle" font-weight="bold">Transport</text>
            <text x="134" y="33" font-size="5" fill="rgba(100,160,255,0.65)" text-anchor="middle">Schicht 3</text>
            <text x="134" y="49" font-size="13" text-anchor="middle">&#128279;</text>
            <rect data-zone="internet" x="7" y="61" width="82" height="46" rx="4" fill="rgba(245,197,24,0.1)" stroke="rgba(245,197,24,0.5)" stroke-width="1"/>
            <text x="48" y="77" font-size="6.5" fill="#f5c518" text-anchor="middle" font-weight="bold">Internet</text>
            <text x="48" y="86" font-size="5" fill="rgba(245,197,24,0.65)" text-anchor="middle">Schicht 2</text>
            <text x="48" y="100" font-size="13" text-anchor="middle">&#127760;</text>
            <rect data-zone="netzzugang" x="93" y="61" width="82" height="46" rx="4" fill="rgba(255,120,80,0.1)" stroke="rgba(255,120,80,0.5)" stroke-width="1"/>
            <text x="134" y="77" font-size="6.5" fill="#ff7850" text-anchor="middle" font-weight="bold">Netzzugang</text>
            <text x="134" y="86" font-size="5" fill="rgba(255,120,80,0.65)" text-anchor="middle">Schicht 1</text>
            <text x="134" y="100" font-size="13" text-anchor="middle">&#128225;</text>
          </svg>
        </div>
        <!-- Lernziele -->
        <div class="s0-lz-box">
          <div class="s0-lz-label">&#127919; Lernziele</div>
          <div class="s0-lz-list">
            <div class="s0-lz-item"><span class="s0-lz-badge">LZ1</span><span>Architektur TCP/IP-Modell verstehen</span></div>
            <div class="s0-lz-item"><span class="s0-lz-badge">LZ2</span><span>Weg eines Datenpakets nachvollziehen</span></div>
            <div class="s0-lz-item"><span class="s0-lz-badge">LZ3</span><span>Rolle von Routern &amp; IP-Adressen</span></div>
            <div class="s0-lz-item"><span class="s0-lz-badge">LZ4</span><span>Internet-Schicht &#8596; OSI Layer 3 zuordnen</span></div>
            <div class="s0-lz-item"><span class="s0-lz-badge">LZ5</span><span>Protokolle den TCP/IP-Schichten zuordnen</span></div>
          </div>
        </div>
      </div>
      <button id="tutorial-start-btn" class="overlay-btn">Tutorial starten &rarr;</button>
    </div>
  </div>
```

- [ ] **Schritt 4.3: Alle Tests laufen lassen — müssen alle PASS**

```bash
npm test
```

Erwartet: Alle Tests PASS, einschließlich der 4 neuen S0-Tests.

- [ ] **Schritt 4.4: Commit**

```bash
git add index.html
git commit -m "feat: S0-Update — Hallenplan-SVG und Lernziele im Intro-Overlay"
```

---

## Task 5: Visuelle Verifikation im Browser

**Files:** keine Code-Änderungen

- [ ] **Schritt 5.1: Dev-Server starten**

```bash
npm run serve
```

Öffne `http://localhost:8080` im Browser.

- [ ] **Schritt 5.2: Overlay prüfen**

Checklist beim Seitenaufruf:
- Overlay erscheint sofort beim Laden
- SVG-Hallenplan links: 4 farbige Zonen mit Name + Schicht-Nr. + Icon sichtbar
- Lernziele rechts: 5 Einträge mit blauem Badge (LZ1–LZ5) + Text
- Button "Tutorial starten →" klickbar → Overlay verschwindet, Spiel startet
- Viewport < 480px (DevTools): Spalten stapeln sich untereinander (1-Spalte)

- [ ] **Schritt 5.3: Dev-Server stoppen**

`Ctrl+C` im Terminal.
